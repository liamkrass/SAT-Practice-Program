// DiceGame placeholder to prevent import errors and prop type issues
const DiceGame = ({ onClose, balance, onEarn }: { onClose: () => void; balance: number; onEarn: (amount: number) => void }) => null;
export default DiceGame;